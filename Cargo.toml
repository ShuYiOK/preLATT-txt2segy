[workspace]
resolver = "2"
members = ["segy-core", "segy-cli", "segy-gui"]

[workspace.package]
version = "0.4.0"
edition = "2021"
license = "MIT"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
