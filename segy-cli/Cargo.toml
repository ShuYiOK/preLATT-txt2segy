[package]
name = "segy-cli"
version.workspace = true
edition.workspace = true

[[bin]]
name = "segy"
path = "src/main.rs"

[dependencies]
segy-core = { path = "../segy-core" }
clap = { version = "4.5", features = ["derive"] }
anyhow = "1.0"
chrono = { version = "0.4", features = ["serde"] }
